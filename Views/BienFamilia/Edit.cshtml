@model SAG2.Models.BienFamilia
@{
    ViewBag.Title = "Editar Familia";
}

<h2>Editar Familia</h2>
@using (Html.BeginForm("Update", "BienFamilia", FormMethod.Post, new { @class = "form-horizontal" }))
{
    @Html.HiddenFor(model => model.ID)
    <div class="form-group">
        <label for="nombre" class="col-sm-2 control-label">Nombre Familia:</label>
        <div class="col-sm-10">
            @Html.TextBoxFor(model => model.Nombre, new { @class = "form-control", placeholder = "Ingrese Nombre Familia", required = "required" })
        </div>
    </div>
    <div class="form-group">
        <div class="col-sm-offset-2 col-sm-10">
            <button type="submit" class="btn btn-default">Actualizar</button>
        </div>
    </div>
}
